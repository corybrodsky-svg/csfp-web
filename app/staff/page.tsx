import SiteShell from "../components/SiteShell";

export default function StaffPage() {
  return (
    <SiteShell title="Staff" subtitle="Staff page test and routing checkpoint.">
      <p>If you can see this, staff routing works.</p>
    </SiteShell>
  );
}
